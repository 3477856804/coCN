# ============================================================
# 自举函数示例.co —— 自举子集「首次支持函数」的最小验证
#
# 验证子集新能力（函数地基）：
#   函数定义（无参数）→ C 函数 / 调用 / 返回 / 输出
# 后续扩展路线：参数 → 作用域/局部变量 → 递归（编译器自举 L2）
# ============================================================

函数 问候语()
    返回 "你好，自举！"
结束

输出 问候语()

函数 加一()
    返回 41 + 1
结束

输出 加一()

函数 判断()
    若 12 > 10 则
        返回 "大"
    结束
    返回 "小"
结束

输出 判断()

输出 "全部完成"