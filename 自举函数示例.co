# ============================================================
# 自举函数示例.co —— 自举子集「函数」的最小验证
#
# 验证子集新能力（函数地基）：
#   函数定义（带参数/返回值）→ C 函数 / 调用 / 返回 / 输出
# 这是 L2 完全自举（让编译器能递归编译自身）的关键一步。
# ============================================================

函数 问候语()
    返回 "你好，自举！"
结束

输出 问候语()

函数 加一()
    返回 41 + 1
结束

输出 加一()

函数 加(整数 a, 整数 b) -> 整数
    返回 a + b
结束

输出 加(3, 5)

函数 减(整数 a, 整数 b) -> 整数
    返回 a - b
结束

输出 减(10, 4)

函数 判断(整数 n) -> 字符串
    若 n > 10 则
        返回 "大"
    结束
    返回 "小"
结束

输出 判断(12)

输出 判断(5)

输出 "全部完成"