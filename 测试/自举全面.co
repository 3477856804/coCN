# 自举全面冒烟：列表、下标、内置函数、否则若、跳出、继续、类型声明
整数 n = 0
列表 表 = []
追加(表, 10)
追加(表, 20)
追加(表, 30)
输出 长度(表)
输出 表[1]
输出 表[长度(表) - 1]
字符串 s = "abcd"
输出 s[0]
输出 s[2]
字符串 词 = 截取(s, 0, 2)
输出 词
输出 替换("aaa", "a", "b")
整数 分 = 85
若 分 >= 90
    输出 "优"
否则若 分 >= 80
    输出 "良"
否则若 分 >= 60
    输出 "中"
否则
    输出 "差"
结束
整数 i = 0
当 i < 10 循环
    i = i + 1
    若 i % 2 == 0
        继续
    结束
    若 i > 5
        跳出
    结束
    输出 i
结束
输出 转字符串(42) + "!"
输出 "全面冒烟通过"
