# 原生通道：列表支持（字面量 / 追加 / 长度 / 下标含负下标 / 输出）
# 解释器侧进功能套件、原生侧由 make native 校验输出逐字节一致
让 表 = []
追加(表, 5)
追加(表, 10)
追加(表, 15)
追加(表, "界")
断言(长度(表) == 4)
断言(表[0] == 5)
断言(表[1] == 10)
断言(表[2] == 15)
断言(表[3] == "界")
断言(表[-1] == "界")
断言(表[-4] == 5)
输出(表)
让 和 = 0
对于 i 从 0 到 长度(表) - 1 循环
    让 项 = 表[i]
    若 类型(项) == "整数" 和 = 和 + 项
结束
断言(和 == 30)
输出(和)
输出 "列表原生测试：全部通过"