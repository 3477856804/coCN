# 代码开发准则（coCN）

本文件记录 coCN 中文编程语言解释器的开发规范。**每次改动代码后必须同步更新本文件对应章节。**

---

## 1. 项目结构

| 文件 / 目录 | 职责 |
|------|------|
| `co.c` | 解释器 + 原生编译后端全部源码（词法 / 语法 / 求值 / 张量与自动求导 / 107 个内置函数 / C 代码生成器），单文件约 6300 行 |
| `co` | 编译产物（不入库，由 `make` 生成） |
| `Makefile` | 构建与分层测试入口（`test` / `native` / `bench` / `asan` / `asan-error` / `tsan` / `check`） |
| `README.md` | 面向用户的语言与 API 文档 |
| `标准库.co` | 用 coCN 自身编写的标准库，通过 `导入 "标准库"` 使用 |
| `例子/*.co` | 可运行示例 |
| `测试/*.co` | 功能回归套件，每个文件必须以 `XXX：全部通过` 结尾 |
| `测试/错误用例/*.co` | 错误路径用例，每个都**必须**非零退出并输出中文诊断 |
| `测试/原生/*.co` | 双通道用例：解释器侧进功能套件与 ASan，原生侧由 `native` 校验输出逐字节一致 |
| `测试/原生一致性.sh` | 原生通道门禁：双通道 stdout/stderr/退出码一致 + 生成 C 零警告 + `--生成C` 产物可独立编译 |
| `测试/基准/性能基准.co` | 性能基准源码。**结果写 stdout（参与一致性比对），耗时写 stderr（不参与）** |
| `测试/基准运行.sh` | 基准运行器：先校验两条通道结果一致，再输出加速比、二进制体积与动态依赖 |

> 基准文件刻意放在 `测试/基准/` 而不是 `测试/` 下：`TESTS` 的 wildcard 会把 `测试/*.co`
> 卷进 ASan 轮次，而基准里有两百万次循环，在 ASan 下要慢十几倍，纯属白等。

---

## 2. 质量门禁（提交前必过）

```bash
make check     # = test + native + bench + asan + asan-error + tsan
```

| 层级 | 命令 | 通过标准 |
|------|------|---------|
| 编译 | `make` | `-Wall -Wextra` 零警告 |
| 功能 | `make test-suite` | `测试/*.co` 与 `测试/原生/*.co` 全部输出「全部通过」 |
| 错误路径 | `make test-error` | `测试/错误用例/*.co` **全部非零退出**，且诊断信息可读 |
| 示例 | `make test-demo` | `例子/*.co` 全部成功（`类型错误.co` 反之必须报错） |
| 原生双通道 | `make native` | 同源码两条通道 **stdout + stderr 逐字节一致**、退出码一致；生成的 C 零警告；错误用例两条通道都拒绝 |
| 基准 | `make bench` | 两条通道计算结果一致；原生产物只依赖 libc/libm |
| 内存 / UB | `make asan` | ASan + UBSan 零泄漏、零未定义行为 |
| 错误路径内存安全 | `make asan-error` | 错误分支零越界读写、零 UB（关闭泄漏检测，因为 `exit(1)` 必有未回收内存） |
| 并发 | `make tsan` | TSan 零数据竞争 |

**Windows / MSYS2 环境的差异**（平台事实，非项目缺陷）：

- MSYS2 的 GCC 不附带任何 sanitizer 运行库（libasan/libubsan/libtsan 全仓库皆无）。
  ASan 门禁在 Windows 上改用 CLANG64 的 clang（compiler-rt 提供 `libclang_rt.asan_dynamic`），
  在 `MSYSTEM=CLANG64` 环境里执行 `make asan` 即可，Makefile 已自动切换 `SAN_CC=clang`。
- TSan 运行库在 Windows 上不存在任何实现（仅 POSIX）。Windows 上 `make tsan`
  会明确声明跳过并说明原因，绝不静默假装检查；POSIX 侧照常执行、覆盖并发正确性。
- 中文路径经子进程传递会做有损代码页转换，所以 `Makefile` 导出
  `MSYS2_ARG_CONV_EXCL=测试;例子`，原生后端先产出到 ASCII 临时名再由宽字符 API 归位。

> `asan-error` 不是可选项。**错误分支是内存问题的重灾区**：张量索引越界曾直接读写堆外内存，
> `longjmp` 跳过 `free` 曾泄漏临时缓冲——这两类问题只有在错误路径上跑净化器才会现形。

> `native` 同样不是可选项。原生后端是**语义的第二套实现**，任何一处对不上就意味着
> 「解释器上测过的代码，编译出来行为不同」——这是最难查的一类问题。
> 双通道逐字节比对是唯一能持续压住它的手段。**stderr 也必须比对**：
> 有了 `输出错误` 之后 stderr 是正式输出通道，只对齐 stdout 等于漏掉一半。

**硬性要求**：新增语言特性必须同时新增功能测试与（若涉及错误分支）错误用例。
只加实现不加测试的改动视为未完成。

---

## 3. 解释器架构

1. **词法**：`advance()` 处理 UTF-8 中文标识符、`#` 注释、多字符符号（`//` `->` `<=` `<<` `>>` …）、
   进制字面量（`0x` / `0b` / `0o`）与 `_` 分隔符。中文关键字在 `advance()` 内以 `strcmp` 链匹配。
   - **多字符符号分支必须 `return`**：函数尾部会把 `text` 覆写成单字符。
   - **中文运算符别名要折算成 ASCII 符号**（`按位与` → `"&"`）：`binary.op` 只有 8 字节，
     装不下 3 个汉字，`strcpy` 会溢出。
   - **进制/十进制字面量都必须用 `strtoull` 检查越界**，`atol` 溢出是未定义行为。
2. **语法**：递归下降，优先级由低到高（与 C 逐层对应，便于对照）
   ```
   parse_expression → parse_or(或) → parse_and(且)
                    → parse_bitor(|) → parse_bitxor(^) → parse_bitand(&)
                    → parse_eq(== !=) → parse_cmp(< > <= >=) → parse_shift(<< >>)
                    → parse_add(+ -) → parse_mul(* / // %)
                    → parse_unary(- + 非 ~ 并发 等待) → parse_postfix([] 调用) → parse_primary
   ```
   **插入新优先级层时必须两处都改**：新层要调用下一层，上一层要改成调用新层。
   只改一处会让整层运算符静默失效（表达式仍能解析，但结合方式是错的）。
3. **求值**：树遍历，`eval_node`（表达式）/ `exec_node`（语句）。
4. **值模型**：`Value` 带**原子**引用计数；`Tensor` 自带 `tref` 与 `grad/children/backward`。

### 3.1 值语义的铁律（违反即为缺陷）

| 规则 | 原因（都曾是真实缺陷） |
|------|------|
| **求值结果绝不原地修改操作数** | `y = -x` 曾原地取负，把 `x` 本身改成 `-5`；`-表[0]` 曾篡改列表元素。取负/取正必须构造新值返回 |
| **比较运算必须在字符串拼接分支之前处理** | `"abc" == "xyz"` 曾走进拼接分支得到 `"abcxyz"`（非空→恒为真），导致断言假绿 |
| **判等一律走 `val_equals`，比较大小一律走 `val_cmp`** | 手写的类型组合分支必然漏情况：曾漏掉布尔、列表、字典、张量 |
| **真值判断一律走 `truthy()`** | 曾有 8 处内联的 `type == VAL_INT ? ival : ...`，互相不一致且不认识新类型 |
| **`list_push` 内部已 retain，调用方不得再 retain** | 双重 retain 造成引用计数永不归零（曾泄漏 72 字节） |
| **跨类型比较大小必须报错** | `"abc" < 5` 静默给出结果比报错更危险 |
| **字符串只允许 `+`** | 曾任何运算符碰到字符串都拼接，`"a" - 1` 静默产出 `"a1"` |
| **容器越界 / 除零 / 空参数必须报错** | 静默返回 0 或空值会让错误传播到很远的地方 |
| **绝不裸读 `Value` 联合体**，一律走 `num_of` / `int_of` / `dim_of` | `平方根("不是数字")` 曾把字符串指针当 `double` 读出，返回 `9.71e+06`。曾有 12 处裸读 |
| **张量下标一律走 `tensor_linear` / `tensor_offset_2d`** | `取(全零([4]), 999)` 曾直接读写堆外内存——这是可利用的内存安全漏洞，不只是"结果不对" |
| **形状维度一律走 `dim_of`** | `全零([0])` 曾 `calloc(0)`，`全零([-5])` 曾申请超大内存 |
| **临时缓冲一律用 `co_tmp_alloc` / `co_tmp_free`** | 普通 `malloc` 会被异常 `longjmp` 跳过而泄漏（`排序` 遇到不可比类型时曾泄漏合并缓冲） |
| **单行 `如果 … 则 … 结束` 的 `结束` 必须归属该 `如果`** | 单行 if 曾不消费同行的 `结束`，于是被外层 `当`/`函数` 抢走，报出「少一个结束」的莫名语法错误 |

### 3.2 类型系统

- `type_matches(annot, v)` 是唯一的类型检查入口，`ensure_type` 负责报错。
- **允许的隐式加宽**：`整数 → 浮点`（无精度损失，与 C/Java/Go 一致）。反向不允许。
- **布尔是独立类型 `VAL_BOOL`**，不是整数别名：
  - 打印为 `真`/`假`，`类型(真)` 返回 `"布尔"`；
  - 标注 `布尔` **只接受** `VAL_BOOL`（`布尔 x = 1` 必须报错）；
  - 但为兼容数值代码，`真 == 1` 为真、`真 + 1 == 2`（`is_numeric_val` / `numeric_of` 把布尔按 0/1 计入）。
- 比较运算符、逻辑运算符（`且` `或` `非`）、谓词类内置函数（`包含` `开头是` `结尾是`
  `文件存在` `删除文件` `全部满足` `任一满足`）**必须返回布尔**。

### 3.3 控制流与并发

- 循环控制标志放在 `LoopContext`，每次迭代开头必须 `ctx.should_continue = 0`
  （否则 `继续` 会退化成 `跳出`——这是真实发生过的缺陷）。
- 函数调用统一走 `invoke_func`：调用期间把 `loop_ctx` 置空，
  使被调函数里的 `跳出`/`继续` 不会污染调用方循环；返回后恢复。
- **栈守卫**（不是写死的层数上限）：`stack_guard_init()` 用 `pthread_getattr_np` 取本线程真实
  栈区间，预留 1/8（至少 1 MB）划出红线；`stack_guard_check()` 在 `invoke_func` / `eval_node` /
  `exec_node` 用 `__builtin_frame_address(0)` 与红线比对，越线报「调用栈即将耗尽」。
  - **必须用 `__builtin_frame_address(0)`，不能用局部变量取地址**：ASan 会把「取过地址的局部变量」
    搬到堆上的假栈（fake stack），量出来的栈位置完全是垃圾。
  - `main` 与 `task_worker` 开头都要调 `stack_guard_init(0)`，否则并发线程无保护。
  - **递归查找必须改成迭代**：`env_get` / `env_get_type` 原本递归遍历作用域链，
    一次变量查找就吃掉数千栈帧，深递归时守卫还没触发就已经栈溢出。
- `g_return_flag` / `g_return_value` / `g_cur_ret_type` / `loop_ctx` / `g_call_depth`
  **必须是 `__thread` 线程局部**，否则并发任务会互相踩踏。
- `并发` / `等待` 是**一元前缀运算符**（在 `parse_unary` 中解析，操作数用 `parse_unary()`）。
  绝不能用 `parse_expression()`——那会让 `等待 甲 + 等待 乙` 把整个加法当成等待的操作数。
- 子线程会读取外层环境链，创建任务前必须 `env_mark_shared(env)` 以启用加锁。
- 引用计数用 `__atomic_add_fetch` / `__atomic_sub_fetch`（`__ATOMIC_ACQ_REL`）。

### 3.4 张量与自动求导

- 张量一律由 `tensor_new(ndim, shape)` 创建（`calloc` 清零），禁止手工 `malloc` 后遗漏
  `tref/grad/children`。
- 所有可微算子创建结果张量时必须 `tensor_set_child`（内部 `tensor_retain`），
  并设置 `backward` 与 `requires_grad`。
- `tensor_backward(root)` 先拓扑排序（`topo_visit`）再从根到叶回传；`ensure_grad` 懒分配梯度缓冲。
- 广播用 `bc_shape` / `bc_child_index`，兼容 `[N,K] ± [1,K]` 偏置广播，反向按广播维度归约。
- `val_free` 对 `VAL_TENSOR` 调 `tensor_free`（引用计数），保证计算图存活到最后一次引用释放。

### 3.5 字符串必须 UTF-8 感知

- 长度、索引、反转、切片、大小写全部走 `utf8_*` 层，按**字符**而非字节处理，绝不切碎汉字。
- 拼接与转换统一用动态构建器 `SBuf`（`sb_init/sb_append/sb_appendf`）+ `val_to_sbuf`。
  **禁止固定大小缓冲区**——`val_to_string` 曾用 4096/8192 字节缓冲，长列表直接溢出。
- 分隔符按**完整子串**匹配，不得用 `strtok`（那是字符集语义，多字节分隔符会错）。
- **字符视图与字节视图必须分开、各自明确**：`长度`/下标/`截取` 按字符；
  `字节数`/`字节`/`字节列表`/`字节转文本` 按字节。编码、散列、协议解析只能走字节视图。
  `字节转文本` 必须拒绝 `0` 字节与超出 `0~255` 的值——否则字符串会被 `\0` 悄悄截断。

### 3.6 位运算与进制的语义约定

| 约定 | 理由 |
|------|------|
| 位运算**只接受整数与布尔**，浮点参与直接报错 | 浮点的位模式没有语言级意义，静默截断是「能跑但结果错」 |
| 移位位数为负或 > 63 **报错** | C 里是未定义行为；不能把 UB 转嫁给用户 |
| 左移用**无符号**运算（`(unsigned long)a << b`） | 有符号左移溢出是 UB；用无符号运算得到确定的补码回绕 |
| 右移**手写算术移位**：`a < 0 ? ~((~(unsigned long)a) >> b) : ...` | C 的负数右移是实现定义；手写保证所有平台一致 |
| `十六进制/二进制/八进制` 负数按 **64 位补码**呈现 | `-0x1` 这种形式没法直接对照寄存器/内存转储 |
| `&&` / `\|\|` 明确报错并指引 `且` / `或` | 与「按位」语义混淆的代价太高，不如直接拦住 |
| 优先级与 C **逐层一致** | 熟悉系统编程的人不必重新背表；不一致会让掩码代码全是暗坑 |

---

## 4. 扩展解释器的操作步骤

**新增关键字**
1. `TokenType` 加枚举 → 2. `advance()` 加映射 → 3. 相应 `parse_*` 处理 → 4. 补测试 → 5. 更新 README 语法表。

**新增内置函数**
1. 实现 `builtin_xxx(int argc, Value **argv)`，参数个数与类型必须校验并报中文错误；
2. 在 `call_builtin` 注册；
3. 谓词返回 `val_bool`，数值返回 `val_int`/`val_flt`；
4. 原地修改类函数返回 `val_retain(list)` 以支持链式；
5. 补 `测试/` 断言 + 必要的错误用例；
6. 更新 `README.md` §4 函数表与函数总数。

**新增二元运算**
1. `TokenType` 加记号 → 2. `advance()` 的符号 `switch` / 中文别名链加映射
（中文别名折算成 ASCII 存入 `op`）→ 3. 新建 `parse_xxx` 层并把优先级链**上下两处**接好 →
4. `eval_node` 的 `ND_BINARY` 加分支；张量走 `tensor_elem_binop`；
**比较类与位运算类都必须放在字符串拼接/数值算术分支之前**（否则会被拼接或算术误吞）→
5. 报错前先 `val_free(l); val_free(r);`（`runtime_error` 会 `longjmp`，之后的释放不会执行）→
6. 补功能测试（含**优先级**断言）+ 错误用例。

**涉及 C 未定义行为的运算必须先拦截**：移位位数（负数 / ≥64）、有符号溢出（左移用无符号运算）、
负数右移（手写算术移位，不依赖实现定义）。UBSan 只能发现你已经写错的，拦截规则才能保证语义确定。

**新增类型**：`ValType` 末尾追加（不要插在中间，`val_cmp` 依赖枚举序做全序）→
`val_to_sbuf` / `val_type_name` / `builtin_type` / `type_matches` / `truthy` / `val_equals` /
`val_cmp` / `val_free` 全部补齐分支。

---

## 5. 已修复的重大缺陷（防止回归）

| 缺陷 | 后果 | 现状 |
|------|------|------|
| `继续` 未重置 `should_continue` | `继续` 等同 `跳出` | 每次迭代开头清零，`测试/边界测试.co` 守护 |
| `转字符串` 是空壳实现 | 返回 NULL 字符串 → 段错误 | 真实转换，`测试/布尔与比较测试.co` 守护 |
| `val_to_string` 固定缓冲 | 长列表/字典缓冲溢出 | 改用 `SBuf` 动态构建 |
| 字符串比较走拼接分支 | `"abc" == "xyz"` 恒为真，**断言假绿** | 比较前置 + `val_equals`，专项测试守护 |
| 列表/字典比较误读联合体 | `[1,2] == [1,2]` 恒为假 | `val_equals` 递归结构化比较 |
| 一元 `-` 原地修改 | `y = -x` 把 `x` 改成 `-5` | 一元运算返回新值 |
| 布尔只是整数别名 | `输出 真` 打印 `1`，类型不可辨 | 独立 `VAL_BOOL` |
| `等待` 用 `parse_expression` | `等待 甲 + 等待 乙` 报"不是任务句柄" | `并发`/`等待` 降为一元运算符 |
| `分割` 用 `strtok` | 多字节分隔符（`、`）被拆错 | 完整子串匹配 |
| `替换` 固定 2× 缓冲 + 空串死循环 | 溢出 / 挂死 | `SBuf` + 空串报错 |
| `dict_set` 覆写泄漏旧键、字典满时静默丢数据 | 泄漏 + 数据丢失 | 复用键 + 满时报错 |
| 字典删除未回填探测链 | 后续查找失败 | `dict_remove` 反向搬移重散列 |
| `取整` 用 `(long)(x+0.5)` | 负数舍入错误 | `llround` + 另加 `向下取整/向上取整/截断` |
| 列表拼接双重 retain | 引用计数永不归零（泄漏 72 字节） | `list_push` 单次 retain |
| 模式匹配手写判等 | 布尔/列表/字典无法匹配 | 统一 `val_equals` |
| 无递归保护 | 无限递归 → 段错误 | 栈守卫按真实剩余栈判定，报「调用栈即将耗尽」且可被捕获 |
| 固定 3000 层递归上限 | ASan 下单帧变大，3000 层就已栈溢出并 ABORT | 改为按真实栈剩余量判定 |
| `env_get` 递归遍历作用域链 | 一次变量查找吃掉数千栈帧，守卫来不及触发 | 改为迭代循环 |
| 12 处裸读 `Value` 联合体 | `平方根("字符串")` 返回 `9.71e+06` 这类**能跑但结果错** | 统一 `num_of` / `int_of` / `dim_of` 强制类型检查 |
| 张量 `取` / `置` 不检查下标 | 直接读写堆外内存（内存安全漏洞） | `tensor_linear` / `tensor_offset_2d` 边界检查 + 负索引 |
| `全零([0])` / `全零([-5])` | `calloc(0)` / 申请超大内存 | `dim_of` 校验正整数并设上限 |
| 形状数组用 `malloc` | `dim_of` 报错 `longjmp` 跳过 `free`，泄漏 4 字节 | 改为栈上 `int sh[CO_MAX_DIM]` |
| `排序` 合并缓冲用 `malloc` | 遇不可比类型抛错时泄漏 | 改用 `co_tmp_alloc`（登记在 `TryFrame`，回滚时释放） |
| 单行 `如果 … 则 … 结束` | 同行 `结束` 被外层块抢走，报「意外的符号 '结束'」 | 单行形式消费同行 `结束`；多余 `结束` 给出可操作提示 |
| 无位运算 | 写不了掩码、协议、编解码，谈不上系统编程 | 完整 `& \| ^ ~ << >>` + 中文别名 + 进制字面量 + 字节视图 |
| **赋值语句用 `env_set`，只看当前作用域** | 函数里 `计数 = 计数 + 1` 在局部新建绑定遮蔽全局：读全局旧值、写局部副本，返回即蒸发。**每次调用都返回 1，全局永远是 0**——不报错、能跑、结果错。计数器/累加器/缓存/分配器全废 | 新增 `env_assign` 沿作用域链原地更新；找不到才在当前作用域新建。`测试/原生/作用域与全局.co` 守护（含遮蔽反向验证：局部声明、形参、循环变量、递归各层局部） |
| 自赋值 `x = x` 先 free 再 retain | 同一对象先被释放，再 retain 已死内存（释放后使用） | `env_assign` 里先 `val_retain_root` 新值再 `val_free` 旧值 |
| `对于` 循环 `步长 0` | `direction` 算成 -1，循环条件立刻不成立，**循环体被静默跳过** | 两条通道都报「循环步长不能为 0」，`测试/错误用例/循环步长为零.co` 守护 |
| 原生 `对于` 循环里 `继续` 跳过自增 | 死循环挂死 | 自增移到 `for(;; _i += _stp)` 的迭代表达式位 |
| 原生 `ng_function` 先绑形参再校验实参个数 | 在越界内存上 `cv_retain(_p[i])`，实打实的 UB | 个数校验前移到绑定之前；另在 `ng_expr_call` 做编译期个数检查（错误左移） |
| 词法器不支持科学计数法 | `1e10` 报「期望 ')' 得到 'e10'」，科学计算写法直接写不出 | 数字扫描支持 `e`/`E` + 可选符号，带前瞻（`整数 e=5` 里的 `e` 仍是变量名），溢出报错 |
| `expect()` 打印 token 的**枚举下标** | 报错写着「期望 26」，无法定位 | `tok_name()` 中文名表（下标与 `TokenType` 声明序严格对齐） |
| `输出(...)` 的 `(` 被当作分组表达式 | `输出("a:", 1, 2)` 报「期望 ')' 得到 ','` | `print_paren_is_arglist()` 前瞻到匹配的 `)` 再看下一个 token，区分分组与实参表 |
| **`空` 能打印却写不出** | `val_to_string` 输出「空」，源码里 `输出(空)` 报「意外的符号 '空'」，round-trip 断裂 | `空` 作为上下文关键字在 `parse_primary` 里识别为空值字面量（类型标注位与表达式位不会撞车） |
| 无 stderr 输出通道 | 诊断信息只能混进 stdout，`co 程序 \| 下游` 被日志污染 | 新增 `输出错误`，两条通道语义逐字对齐 |

---

## 6. 提交规范

- 提交信息用中文简述改动，重大特性须同步更新 `README.md` 与本文件。
- 提交前必须 `make check` 全绿；不得提交 `co` / `co_asan` / `co_tsan` / `core.*` 等产物。
- 推送目标：Gitee 仓库 `COSMOnb666/coCN`。

---

## 7. 原生编译后端

树遍历解释器**在原理上不可能**写内核：内核态没有 libc、没有 `malloc`、没有解释器运行时，
`co.c` 本身跑不起来。要真正「能写内核」，必须让 coCN **编译成原生代码**。

| 阶段 | 目标 | 状态 |
|------|------|------|
| ① `./co --生成C` | AST → 可读的 C 源码，覆盖变量/算术/位运算/控制流/函数/递归 | **已完成** |
| ② `./co --编译` | 调用 `cc` 产出独立二进制，`-Wall -Wextra` 零警告、不链接解释器运行时 | **已完成** |
| ③ 基准与验证 | 斐波那契 / 循环求和 / 素数计数 / 字符串 对比两条通道；验证无额外依赖 | **已完成**（`make bench`） |
| ④ 裸机子集 | 无 GC、无动态分配、无异常的 `裸机` 模式，可 `-ffreestanding` 编译 | 未开始 |

### 7.1 后端结构

代码生成器位于 `co.c` 尾部，以 `ng_` 前缀成组：

| 函数 | 职责 |
|------|------|
| `compile_program_to_c` | 顶层驱动。**第一遍**对每条顶层语句递归 `ng_collect` 收集全局（含 `如果`/`当`/`对于` 体内声明与循环变量），**第二遍**发射函数与主体 |
| `ng_emit_runtime` | 发射自包含的 `CoVal` 运行时（标签联合 + 引用计数 + 全部内置的 C 实现），不依赖解释器 |
| `ng_expr` / `ng_expr_call` | 表达式降级为 C 语句序列 + 临时变量 |
| `ng_stmt` / `ng_if_chain` / `ng_while` / `ng_for` | 语句与控制流 |
| `ng_function` | 单个函数：形参绑定、局部预声明、`_cleanup` 统一释放 |
| `ng_builtin_cname` | coCN 内置名 → 运行时 C 函数名。**必须与解释器 `call_builtin` 的名字逐一对齐** |

### 7.2 后端的硬约束

1. **语义对齐优先于代码好看**。生成的 C 必须复刻解释器的每一条语义：
   `且`/`或` 短路且结果为布尔、`/` 真除得浮点、`//` 向下取整、`%` 用 `fmod`、
   `+` 只做字符串拼接、移位限 0–63、字符串按**字符**坐标索引。
   有分歧时以解释器为准，并**同时**在 `测试/原生/` 加用例。
2. **生成的 C 必须零警告**。使用者会把 `--生成C` 的产物接进自己的构建，噪音等于不可用。
   已处理：完整发射运行时导致的 `-Wunused-function`（`CV_UNUSED` 标注）、
   零参函数的 `-Wunused-parameter`（发射 `(void)_p;`）、
   无 `返回` 的函数的 `-Wunused-label`（`used_cleanup` 标志位控制是否发射 `_cleanup:`）。
3. **静态存储不能调用函数**。全局初值必须写常量初始化式
   （`static CoVal g0 = { CV_NULL, 0, 0.0, NULL, NULL };`，不能写 `cv_null()`）。
4. **能编译期报的错不要留到运行期**。实参个数不匹配已在 `ng_expr_call` 编译期拒绝。
5. **`fputs` 不做格式展开**。运行时字符串里想要一个 `%`，就写 `"%"`；
   误写 `"%%"` 会让 `cv_bin` 拿到 `"%%"` 而永远匹配不上 `"%"`（曾导致取模运算整体失效）。

### 7.3 当前不支持（会在编译期明确报错，不会静默产出错代码）

列表 / 字典 / 张量 / 并发 / 异常 / 模块导入 / 嵌套函数 / 函数值 / 索引赋值。
标量（整数、浮点、字符串、布尔、空）+ 函数 + 递归 + 完整控制流 + 常用内置已全部可用。

### 7.4 实测性能（本机，`make bench`）

| 负载 | 解释器 | 原生 | 加速比 |
|------|-------:|-----:|------:|
| 斐波那契(27) 递归 | 0.387 s | 0.015 s | **25.3×** |
| 循环求和 200 万次 | 0.653 s | 0.083 s | 7.8× |
| 素数计数 60000 | 0.719 s | 0.086 s | 8.4× |
| 字符串拼接/查找 20 万轮 | 0.382 s | 0.093 s | 4.1× |
| 合计 | 2.141 s | 0.278 s | **7.7×** |

原生产物 38 KB，只依赖 libc/libm。函数调用密集的负载收益最大（省掉了
`Env` 分配、字符串键查找与 `Value` 堆分配）；字符串负载收益最小，因为两条通道
都在 `malloc`/`memcpy` 上花时间——这也指明了下一步优化点。

---

## 8. 提交规范

- 提交信息用中文简述改动，重大特性须同步更新 `README.md` 与本文件。
- 提交前必须 `make check` 全绿；不得提交 `co` / `co_asan` / `co_tsan` / `core.*` 等产物。
- 推送目标：Gitee 仓库 `COSMOnb666/coCN`。

---

## 9. 后续方向

- **裸机子集**（§7 阶段 ④）：无 GC、无动态分配的 `裸机` 模式，`-ffreestanding` 可编译。
- **结构体 / 指针 / 内存视图**：固定布局记录与裸内存读写，内核数据结构的前提。
- **FFI**：`外部 函数 ...` 声明直接调用 C 函数与系统调用——原生后端已就位，这是杠杆最大的一步。
- 性能：字节码 VM 取代树遍历；字符串小对象优化（基准里字符串负载只快 4.1×）；OpenBLAS / GPU 后端。
- 语言：闭包与匿名函数、可变参数、切片赋值；原生后端补齐列表/字典。
- 生态：模型序列化（保存/加载参数）、Adam 等优化器、卷积算子、包管理。
