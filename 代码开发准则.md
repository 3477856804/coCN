# 代码开发准则（coCN）

本文件记录 coCN 中文编程语言解释器的开发规范。**每次改动代码后必须同步更新本文件对应章节。**

---

## 1. 项目结构

| 文件 / 目录 | 职责 |
|------|------|
| `co.c` | 解释器全部源码（词法 / 语法 / 求值 / 张量与自动求导 / 96 个内置函数），单文件约 4700 行 |
| `co` | 编译产物（不入库，由 `make` 生成） |
| `Makefile` | 构建与分层测试入口（`test` / `asan` / `tsan` / `check`） |
| `README.md` | 面向用户的语言与 API 文档 |
| `标准库.co` | 用 coCN 自身编写的标准库，通过 `导入 "标准库"` 使用 |
| `例子/*.co` | 可运行示例 |
| `测试/*.co` | 功能回归套件，每个文件必须以 `XXX：全部通过` 结尾 |
| `测试/错误用例/*.co` | 错误路径用例，每个都**必须**非零退出并输出中文诊断 |

---

## 2. 质量门禁（提交前必过）

```bash
make check     # = make test + make asan + make tsan
```

| 层级 | 命令 | 通过标准 |
|------|------|---------|
| 编译 | `make` | `-Wall` 零警告 |
| 功能 | `make test-suite` | `测试/*.co` 全部输出「全部通过」 |
| 错误路径 | `make test-error` | `测试/错误用例/*.co` **全部非零退出**，且诊断信息可读 |
| 示例 | `make test-demo` | `例子/*.co` 全部成功（`类型错误.co` 反之必须报错） |
| 内存 / UB | `make asan` | ASan + UBSan 零泄漏、零未定义行为 |
| 并发 | `make tsan` | TSan 零数据竞争 |

**硬性要求**：新增语言特性必须同时新增功能测试与（若涉及错误分支）错误用例。
只加实现不加测试的改动视为未完成。

---

## 3. 解释器架构

1. **词法**：`advance()` 处理 UTF-8 中文标识符、`#` 注释、多字符符号（`//` `->` `<=` …）。
   中文关键字在 `advance()` 内以 `strcmp` 链匹配。
2. **语法**：递归下降，优先级由低到高
   `parse_expression → parse_or → parse_and → parse_eq → parse_cmp → parse_add → parse_mul → parse_unary → parse_postfix → parse_primary`。
3. **求值**：树遍历，`eval_node`（表达式）/ `exec_node`（语句）。
4. **值模型**：`Value` 带**原子**引用计数；`Tensor` 自带 `tref` 与 `grad/children/backward`。

### 3.1 值语义的铁律（违反即为缺陷）

| 规则 | 原因（都曾是真实缺陷） |
|------|------|
| **求值结果绝不原地修改操作数** | `y = -x` 曾原地取负，把 `x` 本身改成 `-5`；`-表[0]` 曾篡改列表元素。取负/取正必须构造新值返回 |
| **比较运算必须在字符串拼接分支之前处理** | `"abc" == "xyz"` 曾走进拼接分支得到 `"abcxyz"`（非空→恒为真），导致断言假绿 |
| **判等一律走 `val_equals`，比较大小一律走 `val_cmp`** | 手写的类型组合分支必然漏情况：曾漏掉布尔、列表、字典、张量 |
| **真值判断一律走 `truthy()`** | 曾有 8 处内联的 `type == VAL_INT ? ival : ...`，互相不一致且不认识新类型 |
| **`list_push` 内部已 retain，调用方不得再 retain** | 双重 retain 造成引用计数永不归零（曾泄漏 72 字节） |
| **跨类型比较大小必须报错** | `"abc" < 5` 静默给出结果比报错更危险 |
| **字符串只允许 `+`** | 曾任何运算符碰到字符串都拼接，`"a" - 1` 静默产出 `"a1"` |
| **容器越界 / 除零 / 空参数必须报错** | 静默返回 0 或空值会让错误传播到很远的地方 |

### 3.2 类型系统

- `type_matches(annot, v)` 是唯一的类型检查入口，`ensure_type` 负责报错。
- **允许的隐式加宽**：`整数 → 浮点`（无精度损失，与 C/Java/Go 一致）。反向不允许。
- **布尔是独立类型 `VAL_BOOL`**，不是整数别名：
  - 打印为 `真`/`假`，`类型(真)` 返回 `"布尔"`；
  - 标注 `布尔` **只接受** `VAL_BOOL`（`布尔 x = 1` 必须报错）；
  - 但为兼容数值代码，`真 == 1` 为真、`真 + 1 == 2`（`is_numeric_val` / `numeric_of` 把布尔按 0/1 计入）。
- 比较运算符、逻辑运算符（`且` `或` `非`）、谓词类内置函数（`包含` `开头是` `结尾是`
  `文件存在` `删除文件` `全部满足` `任一满足`）**必须返回布尔**。

### 3.3 控制流与并发

- 循环控制标志放在 `LoopContext`，每次迭代开头必须 `ctx.should_continue = 0`
  （否则 `继续` 会退化成 `跳出`——这是真实发生过的缺陷）。
- 函数调用统一走 `invoke_func`：调用期间把 `loop_ctx` 置空，
  使被调函数里的 `跳出`/`继续` 不会污染调用方循环；返回后恢复。
- 递归深度上限 `CO_MAX_CALL_DEPTH = 3000`，超出报「递归过深」而非段错误。
- `g_return_flag` / `g_return_value` / `g_cur_ret_type` / `loop_ctx` / `g_call_depth`
  **必须是 `__thread` 线程局部**，否则并发任务会互相踩踏。
- `并发` / `等待` 是**一元前缀运算符**（在 `parse_unary` 中解析，操作数用 `parse_unary()`）。
  绝不能用 `parse_expression()`——那会让 `等待 甲 + 等待 乙` 把整个加法当成等待的操作数。
- 子线程会读取外层环境链，创建任务前必须 `env_mark_shared(env)` 以启用加锁。
- 引用计数用 `__atomic_add_fetch` / `__atomic_sub_fetch`（`__ATOMIC_ACQ_REL`）。

### 3.4 张量与自动求导

- 张量一律由 `tensor_new(ndim, shape)` 创建（`calloc` 清零），禁止手工 `malloc` 后遗漏
  `tref/grad/children`。
- 所有可微算子创建结果张量时必须 `tensor_set_child`（内部 `tensor_retain`），
  并设置 `backward` 与 `requires_grad`。
- `tensor_backward(root)` 先拓扑排序（`topo_visit`）再从根到叶回传；`ensure_grad` 懒分配梯度缓冲。
- 广播用 `bc_shape` / `bc_child_index`，兼容 `[N,K] ± [1,K]` 偏置广播，反向按广播维度归约。
- `val_free` 对 `VAL_TENSOR` 调 `tensor_free`（引用计数），保证计算图存活到最后一次引用释放。

### 3.5 字符串必须 UTF-8 感知

- 长度、索引、反转、切片、大小写全部走 `utf8_*` 层，按**字符**而非字节处理，绝不切碎汉字。
- 拼接与转换统一用动态构建器 `SBuf`（`sb_init/sb_append/sb_appendf`）+ `val_to_sbuf`。
  **禁止固定大小缓冲区**——`val_to_string` 曾用 4096/8192 字节缓冲，长列表直接溢出。
- 分隔符按**完整子串**匹配，不得用 `strtok`（那是字符集语义，多字节分隔符会错）。

---

## 4. 扩展解释器的操作步骤

**新增关键字**
1. `TokenType` 加枚举 → 2. `advance()` 加映射 → 3. 相应 `parse_*` 处理 → 4. 补测试 → 5. 更新 README 语法表。

**新增内置函数**
1. 实现 `builtin_xxx(int argc, Value **argv)`，参数个数与类型必须校验并报中文错误；
2. 在 `call_builtin` 注册；
3. 谓词返回 `val_bool`，数值返回 `val_int`/`val_flt`；
4. 原地修改类函数返回 `val_retain(list)` 以支持链式；
5. 补 `测试/` 断言 + 必要的错误用例；
6. 更新 `README.md` §4 函数表与函数总数。

**新增二元运算**：接入 `eval_node` 的 `ND_BINARY`；张量走 `tensor_elem_binop`；
比较类必须放在字符串/数值分支**之前**。

**新增类型**：`ValType` 末尾追加（不要插在中间，`val_cmp` 依赖枚举序做全序）→
`val_to_sbuf` / `val_type_name` / `builtin_type` / `type_matches` / `truthy` / `val_equals` /
`val_cmp` / `val_free` 全部补齐分支。

---

## 5. 已修复的重大缺陷（防止回归）

| 缺陷 | 后果 | 现状 |
|------|------|------|
| `继续` 未重置 `should_continue` | `继续` 等同 `跳出` | 每次迭代开头清零，`测试/边界测试.co` 守护 |
| `转字符串` 是空壳实现 | 返回 NULL 字符串 → 段错误 | 真实转换，`测试/布尔与比较测试.co` 守护 |
| `val_to_string` 固定缓冲 | 长列表/字典缓冲溢出 | 改用 `SBuf` 动态构建 |
| 字符串比较走拼接分支 | `"abc" == "xyz"` 恒为真，**断言假绿** | 比较前置 + `val_equals`，专项测试守护 |
| 列表/字典比较误读联合体 | `[1,2] == [1,2]` 恒为假 | `val_equals` 递归结构化比较 |
| 一元 `-` 原地修改 | `y = -x` 把 `x` 改成 `-5` | 一元运算返回新值 |
| 布尔只是整数别名 | `输出 真` 打印 `1`，类型不可辨 | 独立 `VAL_BOOL` |
| `等待` 用 `parse_expression` | `等待 甲 + 等待 乙` 报"不是任务句柄" | `并发`/`等待` 降为一元运算符 |
| `分割` 用 `strtok` | 多字节分隔符（`、`）被拆错 | 完整子串匹配 |
| `替换` 固定 2× 缓冲 + 空串死循环 | 溢出 / 挂死 | `SBuf` + 空串报错 |
| `dict_set` 覆写泄漏旧键、字典满时静默丢数据 | 泄漏 + 数据丢失 | 复用键 + 满时报错 |
| 字典删除未回填探测链 | 后续查找失败 | `dict_remove` 反向搬移重散列 |
| `取整` 用 `(long)(x+0.5)` | 负数舍入错误 | `llround` + 另加 `向下取整/向上取整/截断` |
| 列表拼接双重 retain | 引用计数永不归零（泄漏 72 字节） | `list_push` 单次 retain |
| 模式匹配手写判等 | 布尔/列表/字典无法匹配 | 统一 `val_equals` |
| 无递归保护 | 无限递归 → 段错误 | 3000 层上限，报「递归过深」 |

---

## 6. 提交规范

- 提交信息用中文简述改动，重大特性须同步更新 `README.md` 与本文件。
- 提交前必须 `make check` 全绿；不得提交 `co` / `co_asan` / `co_tsan` / `core.*` 等产物。
- 推送目标：Gitee 仓库 `COSMOnb666/coCN`。

---

## 7. 后续方向

- 性能：字节码 VM 取代树遍历；OpenBLAS / GPU 后端。
- 语言：闭包与匿名函数、异常处理（`尝试`/`捕获`）、结构体或类、可变参数、切片赋值。
- 生态：模型序列化（保存/加载参数）、Adam 等优化器、卷积算子、包管理。
