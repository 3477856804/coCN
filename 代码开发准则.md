# 代码开发准则（coCN）

本文件记录 coCN 项目的中文编程语言解释器开发规范。每次改动代码后必须同步更新本文件对应章节。

---

## 1. 项目结构

| 文件 | 职责 |
|------|------|
| `co.c` | 解释器全部源码（词法 / 语法 / 解释器 / 张量与自动求导 / 内置函数），单文件 |
| `co` | 编译产物（二进制，不入库，由 `make` 生成） |
| `Makefile` | 构建与测试入口 |
| `README.md` | 面向用户的语言与模型 API 文档 |
| `例子/*.co` | 可运行示例（张量演示 / 线性回归 / 异或网络 / 分类演示） |

---

## 2. 构建与测试规范

- 编译：`gcc -O2 -Wall -o co co.c -lm`（必须零警告通过 `-Wall`）。
- 每次提交前必须运行 `make test`，确认四个示例均可训练并收敛（XOR 预测接近 [0,1,1,0]）。
- 新增内置函数后，必须在 `call_builtin` 中注册，并补充到 `README.md` 的 API 表。

---

## 3. 解释器架构（务必遵守）

1. **词法**：`advance()` 处理中文 UTF-8 与 `#` 注释；中文关键字在 `advance()` 内匹配。
2. **语法**：递归下降，`parse_primary → parse_postfix → parse_unary → parse_mul → ... → parse_expression`。
3. **解释**：树遍历 `eval_node`（表达式）/ `exec_node`（语句）。
4. **值模型**：`Value` 带引用计数；`Tensor` 自带 `tref` 引用计数与 `grad/children/backward` 字段。

### 3.1 张量与自动求导约定

- 张量由 `tensor_new(ndim, shape)` 创建，字段用 `calloc` 清零。
- 所有可微算子（`t_add/t_sub/t_mul/t_div/t_matmul/t_transpose/t_sum/t_mean/t_sigmoid/t_tanh/t_relu/t_exp/t_log/t_softmax/t_mse/t_cross_entropy/t_cross_entropy_logits`）
  在创建结果张量时**挂接子节点**（`tensor_set_child`，内部 `tensor_retain`），并设置 `backward` 函数与 `requires_grad`。
- **反向传播**：`tensor_backward(root)` 先拓扑排序（`topo_visit`），再从根到叶调用各节点 `backward`。
  只有 `requires_grad` 的子节点才会累加梯度；`ensure_grad` 懒分配梯度缓冲。
- **内存安全**：`val_free` 对 `VAL_TENSOR` 调用 `tensor_free`（引用计数）；图子节点各自 `tensor_retain`，
  保证计算图存活到最后一次引用释放，且不重复释放。
- **广播**：逐元素算子使用 `bc_shape` / `bc_child_index`，兼容 `[N,K] ± [1,K]` 这类偏置广播；
  反向时按广播维度归约梯度。

### 3.2 修改解释器的硬性规则

- 新增关键字：先在 `TokenType` 加枚举，再在 `advance()` 映射，必要时在 `parse_primary` 处理标识符形态。
- 新增二元运算：在 `eval_node` 的 `ND_BINARY` 分支接入，张量运算统一走 `tensor_elem_binop`。
- 张量元素赋值：`t[i][j] = v` 由 `assign_tensor` 处理；读取 `t[i][j]` 在 `[]` 分支返回标量/行副本。
- 任何新建 `Tensor` 都必须走 `tensor_new`（字段已初始化），禁止手动 `malloc` 张量后遗漏 `tref/grad/children`。

---

## 4. 已知约束与待办

- 解释器为树遍历执行，大规模矩阵（>数千元素）训练较慢，适合教学与原型。
- `for` 循环解析 bug 已修复（原先 `expect(TOK_ID)` 后会多前进一个 token 吞掉 `从`）。
- 后续可扩展：GPU/OpenBLAS 后端、模型序列化（保存/加载参数）、更多优化器（Adam）、卷积算子。

---

## 5. 提交规范

- 提交信息简明说明改动；重大功能（如新增算子族）须同步更新 `README.md` 与本节。
- 推送目标：Gitee 仓库 `COSMOnb666/coCN`。
