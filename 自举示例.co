# 自举示例.co —— 自举编译器（精简子集）编译的目标程序
# 这份源码覆盖：变量、四则/取模、比较、字符串拼接、如果/否则、当、对于。
# 测试脚本会把本文件复制为 自举输入.co，交给 coCN 写的编译器编译成原生二进制，
# 再与解释器 ./co 直接运行本文件逐字节比对输出。
让 全 = 0
让 偶和 = 0
对于 i 从 1 到 10 循环
    全 = 全 + i
    如果 i % 2 == 0 则
        偶和 = 偶和 + i
    结束
结束
输出 全
输出 偶和
让 名 = "自举"
输出 "演示：" + 名
如果 偶和 == 30 则
    输出 "偶数项正确"
否则
    输出 "错误！"
结束
让 次 = 0
当 次 < 2 循环
    输出 次 + 1
    次 = 次 + 1
结束
输出 "共" + 全 + "项和"