# ============================================================
# 并发演示：spawn 多个任务并行求和，再合并结果
# ============================================================

函数 部分和(整数 start, 整数 end)
    整数 s = 0
    对于 i 从 start 到 end 循环
        s = s + i
    结束
    返回 s
结束

# 串行基线
浮点 t0 = 时钟()
整数 串行 = 部分和(0, 2000000)
浮点 t1 = 时钟()
输出 "串行结果 =", 串行, " 用时(秒) =", t1 - t0

# 并发：拆分两段并行计算
浮点 p0 = 时钟()
句柄1 = 并发 部分和(0, 1000000)
句柄2 = 并发 部分和(1000001, 2000000)
整数 a = 等待 句柄1
整数 b = 等待 句柄2
浮点 p1 = 时钟()
输出 "并行结果 =", a + b, " 用时(秒) =", p1 - p0

输出 "理论值 =", (2000001 * 2000000) / 2
输出 "结果一致 =", (a + b) == 串行
